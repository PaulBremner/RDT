using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JackalBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //connect to ROS
    }

    // Update is called once per frame
    void Update()
    {
        //get/display mapping and sensor data + radiation
        //update positions
    }
}
