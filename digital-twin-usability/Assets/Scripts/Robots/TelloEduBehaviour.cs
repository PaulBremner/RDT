using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TelloEduBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //connect to ROS
    }

    // Update is called once per frame
    void Update()
    {
        //get camera feed
    }
}
